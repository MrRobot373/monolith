# Filesystem-scenario overlay: the sandboxed filesystem stack already lives in
# `monolith-base`, so this patch changes only the local tool-result spill
# storage those scenarios exercise.

- id: spill-local
  name: '@monolith/spill-local'
  config:
    root: !!js process.env.MONOLITH_SNAPSHOT_SPILL_ROOT ?? './.spill'

- id: spill-policy
  name: '@monolith/spill-policy'
  config:
    maxInlineBytes: !!js process.env.MONOLITH_SNAPSHOT && 800 || 50000
