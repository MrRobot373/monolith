# Keyless replay counterpart: deterministic HTTP remains real; only the model
# adapter is replaced by replay.
- id: llm-deepseek
  name: '@monolith/llm-deepseek'
  disabled: true

- insert:
    - id: llm-replay
      name: '@monolith/llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
              - id: deepseek-v4-pro

    - id: web-fetch-fixture
      name: './web-fetch-fixture-server.mjs'

- id: web
  name: '@monolith/web'

- id: web-fetch-http
  name: '@monolith/web-fetch-http'
  disabled: true

- id: tool-web
  name: '@monolith/tool-web'
  config:
    search: false
