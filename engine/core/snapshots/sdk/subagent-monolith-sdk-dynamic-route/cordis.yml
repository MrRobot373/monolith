# SDK-profile patch for a deterministic parent model that selects a route for
# a separate SDK child runtime. Both runtimes persist their request headers.

- id: llm-deepseek
  name: '@monolith/llm-deepseek'
  disabled: true

- id: tool-subagent
  name: '@monolith/tool-subagent'
  disabled: true

- id: sdk-jsonrpc-server
  name: '@monolith/sdk-jsonrpc-server'
  disabled: true

- id: session-persistence-jsonl
  name: '@monolith/session-persistence-jsonl'
  config:
    root: !!js monolithHomePath('sessions')
    compression: none

- insert:
    - id: mock-llm
      name: '../../../packages/subagent/subagent-monolith-sdk/tests/fixtures/loader/mock-delegating-llm.ts'
      disabled: !!js process.env.MONOLITH_SNAPSHOT !== 'record'

    - id: subagent-monolith-sdk
      name: '@monolith/subagent-monolith-sdk'
      config:
        profile: sdk
        patches: !!js JSON.parse(process.env.MONOLITH_TEST_CHILD_PATCHES ?? '[]')
        monolithHome: !!js process.env.MONOLITH_TEST_CHILD_HOME
        provider: mock
        model: mock-routed
        env:
          MONOLITH_TELEMETRY_DISABLED: '1'

    - id: subagent-model-selection-settings
      name: '@monolith/tool-subagent/model-selection-settings'
      config:
        enabled: true
        allowedModels:
          - provider: mock
            model: mock-routed

    - id: tool-subagent-monolith-sdk
      name: '../../../packages/subagent/subagent-monolith-sdk/tests/fixtures/loader/scoped-tool-subagent.ts'
      config:
        provider: monolith-sdk
        toolName: subagent
        enableRunInBackground: false
        agentOptions:
          maxTokens: 777
        maxDepth: 'provider-managed'

    - id: sdk-jsonrpc-server-dynamic-live
      name: '@monolith/sdk-jsonrpc-server'
      inject: [sdkAppStartup, loader]
      disabled: !!js process.env.MONOLITH_SNAPSHOT !== 'record'
      config:
        maxTokensAsSuccess: true
