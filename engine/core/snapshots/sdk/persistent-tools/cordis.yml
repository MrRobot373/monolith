# Per-launch SDK customization: retain only persistent Bash and the string
# editor as model-facing tools, with process-local providers and an explicit
# danger-full-access / never-ask policy.

- id: llm-deepseek
  name: '@monolith/llm-deepseek'
  config:
    apiKeyEnv: DEEPSEEK_API_KEY
    streamIdleTimeoutMs: 172800000
    models:
      - id: !!js process.env.MONOLITH_MODEL ?? 'deepseek-v4-flash'
        contextWindow: !!js Number(process.env.MONOLITH_CONTEXT_WINDOW ?? 1000000)

- id: sandbox-policy
  name: '@monolith/sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@monolith/user-approval'
  config:
    policy: never

- id: permission
  name: '@monolith/permission-presets'
  disabled: true

- id: bash-sandbox
  name: '@monolith/bash-sandbox'
  disabled: true

- id: pwsh-sandbox
  name: '@monolith/pwsh-sandbox'
  disabled: true

- id: fs-sandbox
  name: '@monolith/fs-sandbox'
  disabled: true

- id: tool-bash
  name: '@monolith/tool-bash'
  disabled: true

- id: tool-pwsh
  name: '@monolith/tool-pwsh'
  disabled: true

- id: agent-instructions
  name: '@monolith/agent-instructions'
  disabled: true

- id: skill
  name: '@monolith/skill'
  disabled: true

- id: skill-filesystem
  name: '@monolith/skill-filesystem'
  disabled: true

- id: tool-skill
  name: '@monolith/tool-skill'
  disabled: true

- id: tool-jobs
  name: '@monolith/tool-jobs'
  disabled: true

- id: tool-fs
  name: '@monolith/tool-fs'
  disabled: true

- id: tool-fs-search
  name: '@monolith/tool-fs-search'
  disabled: true

- id: tool-subagent-control
  name: '@monolith/tool-subagent-control'
  disabled: true

- id: tool-subagent-list-agents
  name: '@monolith/tool-subagent-control/list-agents'
  disabled: true

- id: tool-subagent
  name: '@monolith/tool-subagent'
  disabled: true

- id: tool-subagent-fork
  name: '@monolith/tool-subagent'
  disabled: true

- id: tool-workflow
  name: '@monolith/tool-workflow'
  disabled: true

- id: tool-todo
  name: '@monolith/tool-todo'
  disabled: true

- id: tool-goal
  name: '@monolith/tool-goal'
  disabled: true

- id: tool-ralph
  name: '@monolith/tool-ralph'
  disabled: true

- id: tool-web
  name: '@monolith/tool-web'
  disabled: true

- id: plan-mode
  name: '@monolith/plan-mode'
  disabled: true

- id: system-prompt
  name: '@monolith/system-prompt'
  config:
    includeHarnessIdentity: false
    persona: !!js process.env.MONOLITH_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

- id: session-persistence-jsonl
  name: '@monolith/session-persistence-jsonl'
  config:
    root: !!js monolithHomePath('sessions')
    compression: none

- insert:
    - id: bash-local
      name: '@monolith/bash-local'

    - id: fs-local
      name: '@monolith/fs-local'
      config:
        cwd: !!js process.cwd()

    - id: terminal
      name: '@monolith/terminal'

    - id: terminal-bash
      name: '@monolith/terminal-bash'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@monolith/tool-bash-persistent'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.
