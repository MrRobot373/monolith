# Keyless parent-model overlay for the MONOLITH SDK diagnostic snapshot.

- id: llm-deepseek
  name: '@monolith/llm-deepseek'
  disabled: true

- insert:
    - id: llm-replay
      name: '@monolith/llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
