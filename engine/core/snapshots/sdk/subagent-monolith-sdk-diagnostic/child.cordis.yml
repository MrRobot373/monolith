# SDK-profile patch for the deterministic diagnostic child runtime.

- id: llm-deepseek
  name: '@monolith/llm-deepseek'
  disabled: true

- id: agent-instructions
  name: '@monolith/agent-instructions'
  disabled: true

- id: system-prompt
  name: '@monolith/system-prompt'
  config:
    persona: 'Return the scripted MONOLITH SDK failure.'

- insert:
    - id: child-mock-llm
      name: '../../../packages/subagent/subagent-monolith-sdk/tests/fixtures/loader/child-mock-llm.ts'
