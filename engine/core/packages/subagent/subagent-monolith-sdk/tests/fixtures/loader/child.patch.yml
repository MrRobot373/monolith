# SDK-profile patch for the child runtime in the cwd-inheritance proof.

- id: llm-deepseek
  name: '@monolith/llm-deepseek'
  disabled: true

- id: system-prompt
  name: '@monolith/system-prompt'
  config:
    persona: 'Echo where you run.'

- id: agent-instructions
  name: '@monolith/agent-instructions'
  disabled: true

- id: skill-filesystem
  name: '@monolith/skill-filesystem'
  config:
    includeDefaultRoots: false

- id: session-persistence-jsonl
  name: '@monolith/session-persistence-jsonl'
  config:
    root: !!js monolithHomePath('sessions')
    compression: none

- insert:
    - id: child-mock-llm
      name: './child-mock-llm.ts'
