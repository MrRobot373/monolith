# One-world invariant for the E2B package composition:
# e2b.cwd and sandbox-policy.workspaceRoot must name the same remote directory,
# which is also bash-local's implicit default workdir.
- id: e2b
  name: '@monolith/e2b'
  config:
    cwd: !!js process.cwd()
    timeoutMs: 180000

- id: subprocess-e2b
  name: '@monolith/subprocess-e2b'

- id: bash
  name: '@monolith/bash-local'
  config:
    timeoutMs: 30000

- id: fs-e2b
  name: '@monolith/fs-e2b'

- id: agents
  name: '@monolith/agent'

- id: session-projection
  name: '@monolith/session-projection'

- id: sandbox-policy
  name: '@monolith/sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: pty
  name: '@monolith/terminal'

- id: terminal-bash
  name: '@monolith/terminal-bash'
  config:
    pollIntervalMs: 25
    exactProbeAfterMs: 150
    idleSilenceMs: 2000
    handoffGraceMs: 500
    timeoutMs: 5000
    disposeGraceMs: 1000

- id: lsp
  name: '@monolith/lsp'

- id: lsp-stdio
  name: '@monolith/lsp-stdio'
  config:
    servers:
      fixture:
        command: node
        args:
          - !!js process.cwd() + '/fixture-lsp.mjs'
        extensionToLanguage:
          .ts: typescript
        shutdownTimeoutMs: 1000
        killGraceMs: 500
