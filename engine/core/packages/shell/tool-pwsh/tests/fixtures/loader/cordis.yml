# Minimal tool-pwsh composition: real app boot path, real pwsh executor, real
# foreground + background tool calls; driven by the package's loader.spec.ts.
- id: system-prompt
  name: '@monolith/system-prompt'

- id: tools
  name: '@monolith/tools'

- id: subprocess
  name: '@monolith/subprocess-local'

- id: bash
  name: '@monolith/pwsh-local'
  config:
    graceMs: 200

- id: shell-env
  name: '@monolith/shell-env'

- id: tasks
  name: '@monolith/jobs-local'

- id: tool-jobs
  name: '@monolith/tool-jobs'

- id: tool-pwsh
  name: '@monolith/tool-pwsh'
