/**
 * @monolith/base — the shared monolith core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `monolith.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @monolith/base
 */

export {}
