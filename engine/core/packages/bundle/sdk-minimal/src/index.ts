/**
 * @monolith/sdk-minimal — the standalone minimal SDK profile bundle.
 * The package's substance is `cordis.patch.yml`, declared by the
 * `monolith.bundle.patch` manifest field and resolved by the profile composer;
 * this module carries no runtime interface.
 * @module @monolith/sdk-minimal
 */

export {}
