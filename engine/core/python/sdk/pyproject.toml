[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "monolith-sdk"
version = "0.0.0.dev0"
description = "Python SDK for MONOLITH"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "MONOLITH" }]
dependencies = [
  "pydantic>=2.12,<3",
  "monolith-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/MrRobot373/monolith"
Documentation = "https://github.com/MrRobot373/monolith/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/MrRobot373/monolith/issues"
Source = "https://github.com/MrRobot373/monolith"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/monolith"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
monolith-runtime-bin = { path = "../sdk-runtime", editable = true }
